tensorflow==2.18.0
numpy==1.24.3  # Updated to a version compatible with Python 3.11
flask==2.2.3   # Keeping as is since it was working
werkzeug==2.2.3  # Keeping as is since it was working
pillow==9.2.0  # Keeping as is since it was working
gunicorn
